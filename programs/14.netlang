set packet: Packet <- {
    payload <- "Hello!",
    protocol <- ICMP,
    size <- 1600
}
set h1: Host <- {
    name <- "Host 1",
    ports <- [
        CopperEthernetPort {
            portId <- "eth0",
            ip <- 192.168.1.5/24
        }
    ]
}
//set hosty: [Host] <- [h1]
print h1.ports

print packet.payload
print packet.size
print packet

send packet from h1.eth0 to 192.168.2.0